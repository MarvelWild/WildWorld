--[[ search lua doc from ide

Function
Synopsis
files = love.filesystem.getDirectoryItems( dir )
Arguments
string dir
The directory.
Returns
table files
A sequence with the names of all files and subdirectories as strings.

example:
