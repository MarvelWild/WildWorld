local _=
{
	frame=0,
	windowHeight=512,
	windowWidth=512,
	scale=Config.scale,
	uiScale=Config.scale,
	isClient=false,
	login="defaultLogin",
	selectedEntity=nil,
	isOnHorse=true, -- tmp kiss
	hasErrors=false,
	hasWarnings=false,
}

return _